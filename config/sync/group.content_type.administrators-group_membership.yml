langcode: en
status: true
dependencies:
  config:
    - group.type.administrators
  module:
    - user
id: administrators-group_membership
label: 'Administrators: Group membership'
description: 'Adds users to groups as members.'
group_type: administrators
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
