{#
/**
* @file
* Default theme implementation to display the file entity as a video tag.
*
* Available variables:
* - attributes: An array of HTML attributes, intended to be added to the
*   video tag.
* - files: And array of files to be added as sources for the video tag. Each
*   element is an array with the following elements:
*   - file: The full file object.
*   - source_attributes: An array of HTML attributes for to be added to the
*     source tag.
*
* @ingroup themeable
*/
#}
<video crossorigin {{ attributes }} controlslist="nodownload">
  {% for file in files %}
    <source {{ file.source_attributes }} />
  {% endfor %}
  {% if tracks %}
    {% for track in tracks %}
      <track {{ track.track_attributes }} />
    {% endfor %}
  {% endif %}
</video>
{% if tracks %}
<div><a href="{{ tracks[0].track_attributes['src'] }}" type="button" class="btn btn-md btn-gray" target="_blank" rel="noopener noreferrer">Download default caption file</a></div>
{% endif %}