persistent_identifiers.settings:
  type: config_object
  label: 'Persistent Identifier Settings'
  mapping:
    pi_enabled_plugins:
      type: sequence
      label: 'List of persistent identifier plugins which are enabled'
      sequence:
        type: string
